# Product Service
The Product Service is a simple web service built using Javascript and the Node runtime environment. It is responsible for serving the product catalog, which includes a list of products that can be fetched via a RESTful API.

## Testing
1. Install the REST Client extension in VS Code to use .http files.

2. Use the provided test-product-service.http file to test the ser